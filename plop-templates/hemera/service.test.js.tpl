import test from 'ava'

test('service: valid message', t => {
  // test service using valid message
  t.pass()
})

test('service: invalid message', t => {
  // confirm message is invalid
  t.pass()
})